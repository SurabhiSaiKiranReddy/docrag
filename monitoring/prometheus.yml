global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: docrag
    metrics_path: /metrics
    static_configs:
      # 'api' is the DocRAG service name in docker-compose; falls back to host.
      - targets: ["api:8000"]
